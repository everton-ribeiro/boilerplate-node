import { type Config } from "prettier";

const config: Config = {
    trailingComma: "es5",
    tabWidth: 2,
    semi: true,
    singleQuote: false,
    arrowParens: "avoid",
    printWidth: 80,
};
export default config

